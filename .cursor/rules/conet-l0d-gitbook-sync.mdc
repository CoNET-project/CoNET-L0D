---
description: conet-l0d 公共语义必须同步 web3:// 产品、协议与 Linux 开发文档
---

# conet-l0d 与 GitBook 同步

改动白皮书、`RULES.md`、MVP、CLI、公开 config 或运行时生命周期时，
同一任务按受影响层同步：

1. `applications/web3-url.md` — 跨平台 `web3://` 产品模型；
2. `l0/web3-application-protocol.md` — locator、签名信封、响应与持续数据流合同；
3. `developers/conet-l0d.md` — Linux 安装、CLI、config 与诊断。

`applications/conet-l0d.md` 仅保留旧链接指针，禁止再次扩写成平行产品页。
公开正文使用英语。白皮书英中版本必须同行。

## 边界

- `web3://` 是使用 L0 基础设施的应用协议。
- `conet-l0d` 只是 Linux server/client runtime。
- Windows、macOS、Android、iOS 与浏览器实现客户端协议，不依赖 Linux runtime。
- 只有底层 L0 wire / SI 命令变化时，才同步 `using-l0`、mailbox routing
  与 SI developer guide。
- 应用 offer、accept 与 stream frame 不是 SI 命令。

## 严禁

- ❌ 在多个页面重复一套 `conet-l0d` 产品叙述
- ❌ 把 Linux runtime 定位成 L1 共识产品
- ❌ 把底层 Linux 本地网络实现细节写成公开使用合同
- ❌ 把未上线的 `p2p_stream_*`、`duplex_*` 或新 `listenKind` 写成现役 SI
- ❌ 为文档发明新域名，或未获要求运行 `deployGitbook.sh`
